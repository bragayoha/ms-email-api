"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const ace_1 = require("@adonisjs/ace");
/**
 * Launch queue workers to start processing
 *
 * @version 2.0.0
 * @adonis-version 5.0+
 */
class SchedulerRun extends ace_1.BaseCommand {
    constructor(app, kernel) {
        super(app, kernel);
        this.scheduler = app.container.use('Adonis/Addons/Scheduler');
    }
    /**
     * Execute command
     */
    async run() {
        this.logger.success('Scheduler are running...');
        await this.scheduler.run();
    }
}
exports.default = SchedulerRun;
SchedulerRun.commandName = 'scheduler:run';
SchedulerRun.description = 'Start one or more workers';
SchedulerRun.settings = {
    loadApp: true,
    stayAlive: true,
};
